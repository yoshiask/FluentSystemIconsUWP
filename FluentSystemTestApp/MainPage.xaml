<Page
    x:Class="FluentSystemTestApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FluentSystemTestApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:fluent="using:Fluent.Icons.Compact"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <StaticResource x:Key="NavigationViewDefaultPaneBackground" ResourceKey="SystemControlAcrylicWindowBrush"/>
        <StaticResource x:Key="NavigationViewExpandedPaneBackground" ResourceKey="SystemControlAcrylicWindowBrush"/>
    </Page.Resources>
    
    <NavigationView>
        <NavigationView.MenuItems>
            <NavigationViewItem Content="Navigate">
                <NavigationViewItem.Icon>
                    <fluent:FluentSymbolIcon Symbol="Directions24"/>
                </NavigationViewItem.Icon>
            </NavigationViewItem>
            <NavigationViewItem Content="Discover">
                <NavigationViewItem.Icon>
                    <fluent:FluentSymbolIcon Symbol="Pin16"/>
                </NavigationViewItem.Icon>
            </NavigationViewItem>
            <NavigationViewItem Content="Explore">
                <NavigationViewItem.Icon>
                    <fluent:FluentSymbolIcon Symbol="Home16"/>
                </NavigationViewItem.Icon>
            </NavigationViewItem>
            <NavigationViewItem Content="Favorites">
                <NavigationViewItem.Icon>
                    <fluent:FluentSymbolIcon Symbol="Star16"/>
                </NavigationViewItem.Icon>
            </NavigationViewItem>
        </NavigationView.MenuItems>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <ListView ItemsSource="{x:Bind Symbols, Mode=OneWay}">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="fluent:FluentSymbol">
                        <ListViewItem>
                            <StackPanel Spacing="10" Orientation="Horizontal">
                                <fluent:FluentSymbolIcon Symbol="{x:Bind}" FontSize="32"/>
                                <TextBlock Text="{x:Bind}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </ListViewItem>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <StackPanel x:Name="ButtonPanel" Spacing="10" Grid.Column="1">
                <Button Style="{ThemeResource ButtonRevealStyle}"
                    VerticalAlignment="Center" HorizontalAlignment="Center">
                    <StackPanel Spacing="5" Orientation="Horizontal">
                        <fluent:FluentSymbolIcon Symbol="FolderAdd16" FontSize="64"/>
                        <TextBlock Text="New folder" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>
    </NavigationView>

    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton Label="Add">
                <AppBarButton.Icon>
                    <fluent:FluentSymbolIcon Symbol="Add16"/>
                </AppBarButton.Icon>
            </AppBarButton>
            <AppBarButton Label="Edit">
                <AppBarButton.Icon>
                    <fluent:FluentSymbolIcon Symbol="Edit16"/>
                </AppBarButton.Icon>
            </AppBarButton>
            <AppBarButton Label="Share">
                <AppBarButton.Icon>
                    <fluent:FluentSymbolIcon Symbol="Share16"/>
                </AppBarButton.Icon>
            </AppBarButton>

            <CommandBar.SecondaryCommands>
                <AppBarButton Label="Add">
                    <AppBarButton.Icon>
                        <fluent:FluentSymbolIcon Symbol="Add16Filled"/>
                    </AppBarButton.Icon>
                </AppBarButton>
                <AppBarButton Label="Edit">
                    <AppBarButton.Icon>
                        <fluent:FluentSymbolIcon Symbol="Edit16Filled"/>
                    </AppBarButton.Icon>
                </AppBarButton>
                <AppBarButton Label="Share">
                    <AppBarButton.Icon>
                        <fluent:FluentSymbolIcon Symbol="Share16Filled"/>
                    </AppBarButton.Icon>
                </AppBarButton>
            </CommandBar.SecondaryCommands>
        </CommandBar>
    </Page.BottomAppBar>
</Page>
